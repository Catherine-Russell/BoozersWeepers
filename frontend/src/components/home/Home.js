import React from 'react';
import NavBar from '../NavBar/NavBar';

const Home = ({ navigate }) => {
  return(
    <div>
      <NavBar/>
   
<h1>this is the home page for now</h1>
    Sign-Up
    </div>
  )
}

export default Home;
